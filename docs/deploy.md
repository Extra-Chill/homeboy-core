# deploy

Build and deploy components to a remote server.

```bash
homeboy deploy <project> <server> <component>
homeboy deploy <project> <server> <component> --dry-run
```

Use `homeboy help deploy` for the authoritative flag list.
