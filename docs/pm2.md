# pm2

Run PM2 commands on a configured server.

```bash
homeboy pm2 <server> list
homeboy pm2 <server> restart <name>
homeboy pm2 <server> logs <name>
```

Use `homeboy help pm2` for the authoritative flag list.
