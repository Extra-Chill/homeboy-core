# `homeboy list`

## Synopsis

```sh
homeboy list
```

## Description

`homeboy list` is an alias command that prints the same help text as `homeboy --help`.

This command exists as a convenience entrypoint (for example when you want a quick command list without remembering `--help`).

## Output

This command prints clap-generated help text to stdout.

- Output is plain text (no JSON envelope).

## Exit code

- `0` on success.

## Related

- [Root command](../cli/homeboy-root-command.md)
- [Commands index](commands-index.md)
