# db

Database utilities for WordPress / MySQL on a configured server.

```bash
homeboy db tables <project> <server>
homeboy db query <project> <server> --sql <sql>
homeboy db dump <project> <server> --out <path>
```

Use `homeboy help db` for the authoritative flag list.
