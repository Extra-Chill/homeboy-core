# pin

Pin frequently used items (projects, servers, tables, files).

```bash
homeboy pin list
homeboy pin add <type> <id>
homeboy pin remove <type> <id>
```

Use `homeboy help pin` for the authoritative flag list.
