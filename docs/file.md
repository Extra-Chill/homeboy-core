# file

Remote file operations for a configured server.

```bash
homeboy file list <server> <path>
homeboy file get <server> <path>
homeboy file put <server> <path> --from <local-path>
```

Use `homeboy help file` for the authoritative flag list.
